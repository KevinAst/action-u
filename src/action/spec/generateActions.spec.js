import expect              from 'expect';
import { generateActions } from '../../../tooling/ModuleUnderTest';

describe('reducerHash() tests', () => {

  it('stub test', () => {
    expect(generateActions()).toBe(123);
  });

});
